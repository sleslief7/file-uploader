import { Center } from '@chakra-ui/react';
import LoginForm from '../forms/LoginForm';

const LoginPage = () => {
  return (
    <Center flex="1">
      <LoginForm />
    </Center>
  );
};

export default LoginPage;
